# Session Directory

This directory contains development session logs for AI coding agents.

## Structure

Each session should create a directory with a unique session ID.

## Usage

AI agents should:
1. Create a session directory at the start
2. Log context, changes, and decisions
3. Reference session in commits/PRs
